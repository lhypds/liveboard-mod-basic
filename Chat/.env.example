# The sc bridge this card talks to: a server that runs the `sc` (simple-ai-chat) CLI and
# exposes it over HTTP. It is not part of this board — the board only points at it.
# See the Chat section of the module README for the two endpoints the card uses.
VITE_SC_BRIDGE_URL=
