# These VITE_ values are compiled into the browser bundle. Restrict both
# credentials to the board's production origin in their provider consoles.
# The map itself needs only the Mapbox token; the address search box appears
# once the Google Maps key is set as well.
VITE_MAPBOX_TOKEN=
VITE_GOOGLE_MAPS_API_KEY=
